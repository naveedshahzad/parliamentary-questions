- name = errors.class.model_name.human
- errors = errors.errors
- if errors.any?
  .pq-msg-error
    = "#{pluralize(errors.count, "error")} prohibido para que esta #{name} se guarde:"
    ul
      - errors.full_messages.each do |msg|
        li= msg
