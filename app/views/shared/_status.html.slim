.status
  - if status.deleted?
    span.fa.fa-times-circle&nbsp;
    = (defined? inactive) ? inactive : ' Inactivo'
  - else
    span.fa.fa-check-circle&nbsp;
    = (defined? active) ? active : ' Activo'
  
