#admin-wl-form
  = form_for @early_bird_member do |f|
    = render partial: 'shared/errors', object: @early_bird_member
    - if @early_bird_member.errors.any?
    fieldset
      .form-group
        label.form-label for="early_bird_member_name"  Nombre
        = f.text_field :name, {class: 'form-control'}
      .form-group
        label.form-label for="early_bird_member_email"  Email
        = f.email_field :email , {class: 'form-control'}
      p.text= t('.status')
      .form-group
        label.block-label for="early_bird_member_deleted"
          = f.check_box :deleted
          = t('.inactive')
      .form-group
        = f.submit 'Guardar', :id => 'save', :class => 'button'
